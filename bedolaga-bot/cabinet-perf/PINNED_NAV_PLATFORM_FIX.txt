Pinned bottom nav + PlatformProvider fix

Live: index-d1377d3f.js / index-830cf3f3.css

1) CSS: removed #root{transform:translateZ(0)} breaking position:fixed bottom nav.
2) usePlatform (we): never throws; returns Yc()/safe web fallback if context missing.
3) Patched ALL historical index-*.js on server (stale Telegram caches).
4) ErrorBoundary Hn(): treats PlatformProvider errors as chunk-reload (auto refresh).
5) Support chunk cache-bust + Caddy static max-age=60 must-revalidate.
