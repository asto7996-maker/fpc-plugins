Pinned bottom nav + PlatformProvider fix + blackscreen fix

Live: index-119ecf90.js / index-830cf3f3.css

1) Sticky nav: removed #root transform containing block.
2) usePlatform: return e||Yc() (no throw).
3) BLACK SCREEN FIX: never put ?query on ES module entry src — it duplicates
   the module graph vs chunk imports and blanks the app.
