"""
Инструкции по операционным системам: шаги плюс пояснения к частым затыкам.
"""

from __future__ import annotations

from handlers.style import footer_hint, header, soft_rule, step

GUIDE_INTRO = (
    f"{header('📖', 'Гайд по подключению')}\n\n"
    f"Выберите свою систему, и я пришлю короткую инструкцию: какое приложение "
    f"поставить, куда вставить ключ и что включить. Весь процесс занимает "
    f"примерно минуту, а ключ берётся из раздела «🔑 Мой ключ».\n\n"
    f"{footer_hint('кнопки ниже')}"
)

GUIDES: dict[str, str] = {
    "ios": (
        f"{header('🍎', 'Подключение на iPhone и iPad')}\n\n"
        f"{step(1, 'Установите Happ из App Store')}\n"
        f"{step(2, 'Скопируйте ключ из бота — раздел «🔑 Мой ключ»')}\n"
        f"{step(3, 'В Happ нажмите «+» и выберите вставку из буфера')}\n"
        f"{step(4, 'Включите профиль и разрешите добавить VPN-конфигурацию')}\n\n"
        f"iOS один раз спросит разрешение на VPN и код-пароль устройства — "
        f"это стандартное системное окно, так и должно быть. "
        f"После включения в статус-баре появится значок VPN.\n\n"
        f"{soft_rule()}\n"
        f"Если сайты не открываются, выключите и снова включите профиль "
        f"в Happ либо выберите другой сервер."
    ),
    "android": (
        f"{header('🤖', 'Подключение на Android')}\n\n"
        f"{step(1, 'Установите Happ (подойдут также v2rayNG и Hiddify)')}\n"
        f"{step(2, 'Скопируйте ключ из бота — раздел «🔑 Мой ключ»')}\n"
        f"{step(3, 'Нажмите «+» и импортируйте конфигурацию из буфера')}\n"
        f"{step(4, 'Нажмите «Подключиться» и подтвердите запрос системы')}\n\n"
        f"Android попросит разрешение на создание VPN-подключения — "
        f"его нужно принять, иначе трафик не пойдёт. "
        f"На некоторых прошивках стоит отключить экономию батареи для клиента, "
        f"иначе система будет обрывать соединение в фоне.\n\n"
        f"{soft_rule()}\n"
        f"Если подключение есть, а интернета нет, смените режим маршрутизации "
        f"в настройках клиента или попробуйте Hiddify."
    ),
    "windows": (
        f"{header('💻', 'Подключение на Windows')}\n\n"
        f"{step(1, 'Скачайте Happ, Hiddify или v2rayN')}\n"
        f"{step(2, 'Скопируйте ключ из бота — раздел «🔑 Мой ключ»')}\n"
        f"{step(3, 'Добавьте профиль из буфера обмена')}\n"
        f"{step(4, 'Нажмите Connect и дождитесь статуса подключения')}\n\n"
        f"Для браузера обычно достаточно системного прокси — включите "
        f"System Proxy в клиенте, и трафик пойдёт через VPN без "
        f"дополнительных настроек. Если нужен весь трафик компьютера, "
        f"выберите режим TUN.\n\n"
        f"{soft_rule()}\n"
        f"Антивирус или брандмауэр может блокировать клиент — "
        f"при проблемах добавьте приложение в исключения."
    ),
    "macos": (
        f"{header('🖥', 'Подключение на macOS')}\n\n"
        f"{step(1, 'Установите Happ, Streisand или Hiddify')}\n"
        f"{step(2, 'Скопируйте ключ из бота — раздел «🔑 Мой ключ»')}\n"
        f"{step(3, 'Импортируйте конфигурацию из буфера обмена')}\n"
        f"{step(4, 'Подключитесь и подтвердите добавление VPN')}\n\n"
        f"macOS запросит пароль администратора при первой установке "
        f"системного расширения — это нормально и требуется один раз. "
        f"Дальше подключение включается одним нажатием из меню.\n\n"
        f"{soft_rule()}\n"
        f"Если приложение не запускается, разрешите его в разделе "
        f"«Конфиденциальность и безопасность» системных настроек."
    ),
}

OS_TITLES = {
    "ios": "🍎 iOS",
    "android": "🤖 Android",
    "windows": "💻 Windows",
    "macos": "🖥 macOS",
}
