# Ядро автоматизации браузера (async API).
playwright>=1.44.0

# Лёгкие HTTP-запросы и парсинг DOM для быстрых операций без браузера.
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.2.0

# Загрузка переменных окружения из .env (опционально, но рекомендуется).
python-dotenv>=1.0.0
